cd work\smatest
cd act\smatest

git add .
git commit -m "restore"
git push origin main
vercel --prod

git add .
git commit -m "テスト変更"
git push origin develop

git checkout develop
git pull origin main
git merge main
git push origin develop

ローカルでマージ:
mainブランチに切り替えます。

git checkout main
git pull origin main
git merge develop
git push origin main

Vercelが自動で本番環境（https://smashcity.jp/api/）を更新します。




google analytics